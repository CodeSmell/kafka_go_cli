# Internal packages

Use `internal/` for application code that should not be imported by external modules.

Planned packages:

- `internal/config` for config loading and validation
- `internal/cli` for command wiring
- `internal/app` for runtime lifecycle
